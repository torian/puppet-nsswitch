###############################################################################
# << FILE MANAGED BY PUPPET >>
# Manual changes are likey to be overwritten
###############################################################################

uid nslcd
gid ldap

uri  <%= @uri %>
base <%= @base %>

timelimit      5
bind_timelimit 10
idle_timelimit 3600

ssl no
tls_cacertdir /etc/openldap/cacerts
